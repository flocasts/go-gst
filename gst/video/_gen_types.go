// Code generated by gst-bindgen. DO NOT EDIT.

package video

/*
#include "gst.go.h"
*/
import "C"

import (
	"unsafe"

	"github.com/go-gst/go-glib/glib"
)

// ColorBalanceChannel:
// The #GstColorBalanceChannel object represents a parameter
// for modifying the color balance implemented by an element providing the
// #GstColorBalance interface. For example, Hue or Saturation.
type ColorBalanceChannel struct{ *glib.InitiallyUnowned }

func wrapColorBalanceChannel(obj *glib.Object) *ColorBalanceChannel {
	return &ColorBalanceChannel{&glib.InitiallyUnowned{Object: obj}}
}

// FromGstColorBalanceChannelUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstColorBalanceChannelUnsafeNone(ptr unsafe.Pointer) *ColorBalanceChannel {
	if ptr == nil {
		return nil
	}
	return wrapColorBalanceChannel(glib.TransferNone(ptr))
}

// FromGstColorBalanceChannelUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstColorBalanceChannelUnsafeFull(ptr unsafe.Pointer) *ColorBalanceChannel {
	if ptr == nil {
		return nil
	}
	return wrapColorBalanceChannel(glib.TransferFull(ptr))
}

// Instance returns the native C GstColorBalanceChannel pointer.
func (c *ColorBalanceChannel) Instance() *C.GstColorBalanceChannel { return C.toGstColorBalanceChannel(c.Unsafe()) }

// VideoAggregator:
// VideoAggregator can accept AYUV, ARGB and BGRA video streams. For each of the requested
// sink pads it will compare the incoming geometry and framerate to define the
// output parameters. Indeed output video frames will have the geometry of the
// biggest incoming video stream and the framerate of the fastest incoming one.
// 
// VideoAggregator will do colorspace conversion.
// 
// Zorder for each input stream can be configured on the
// #GstVideoAggregatorPad.
type VideoAggregator struct{ Aggregator *base.Aggregator }

func wrapVideoAggregator(obj *glib.Object) *VideoAggregator {
	return &VideoAggregator{Aggregator: base.FromGstAggregatorUnsafeNone(unsafe.Pointer(obj.GObject))}
}

// FromGstVideoAggregatorUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoAggregatorUnsafeNone(ptr unsafe.Pointer) *VideoAggregator {
	if ptr == nil {
		return nil
	}
	return wrapVideoAggregator(glib.TransferNone(ptr))
}

// FromGstVideoAggregatorUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoAggregatorUnsafeFull(ptr unsafe.Pointer) *VideoAggregator {
	if ptr == nil {
		return nil
	}
	return wrapVideoAggregator(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoAggregator pointer.
func (v *VideoAggregator) Instance() *C.GstVideoAggregator { return C.toGstVideoAggregator(v.Unsafe()) }

// VideoAggregatorConvertPad:
// An implementation of GstPad that can be used with #GstVideoAggregator.
// 
// See #GstVideoAggregator for more details.
type VideoAggregatorConvertPad struct{ VideoAggregatorPad *VideoAggregatorPad }

func wrapVideoAggregatorConvertPad(obj *glib.Object) *VideoAggregatorConvertPad {
	return &VideoAggregatorConvertPad{VideoAggregatorPad: wrapVideoAggregatorPad(obj)}
}

// FromGstVideoAggregatorConvertPadUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoAggregatorConvertPadUnsafeNone(ptr unsafe.Pointer) *VideoAggregatorConvertPad {
	if ptr == nil {
		return nil
	}
	return wrapVideoAggregatorConvertPad(glib.TransferNone(ptr))
}

// FromGstVideoAggregatorConvertPadUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoAggregatorConvertPadUnsafeFull(ptr unsafe.Pointer) *VideoAggregatorConvertPad {
	if ptr == nil {
		return nil
	}
	return wrapVideoAggregatorConvertPad(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoAggregatorConvertPad pointer.
func (v *VideoAggregatorConvertPad) Instance() *C.GstVideoAggregatorConvertPad { return C.toGstVideoAggregatorConvertPad(v.Unsafe()) }

type VideoAggregatorPad struct{ AggregatorPad *base.AggregatorPad }

func wrapVideoAggregatorPad(obj *glib.Object) *VideoAggregatorPad {
	return &VideoAggregatorPad{AggregatorPad: base.FromGstAggregatorPadUnsafeNone(unsafe.Pointer(obj.GObject))}
}

// FromGstVideoAggregatorPadUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoAggregatorPadUnsafeNone(ptr unsafe.Pointer) *VideoAggregatorPad {
	if ptr == nil {
		return nil
	}
	return wrapVideoAggregatorPad(glib.TransferNone(ptr))
}

// FromGstVideoAggregatorPadUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoAggregatorPadUnsafeFull(ptr unsafe.Pointer) *VideoAggregatorPad {
	if ptr == nil {
		return nil
	}
	return wrapVideoAggregatorPad(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoAggregatorPad pointer.
func (v *VideoAggregatorPad) Instance() *C.GstVideoAggregatorPad { return C.toGstVideoAggregatorPad(v.Unsafe()) }

// VideoAggregatorParallelConvertPad:
// An implementation of GstPad that can be used with #GstVideoAggregator.
// 
// See #GstVideoAggregator for more details.
type VideoAggregatorParallelConvertPad struct{ VideoAggregatorConvertPad *VideoAggregatorConvertPad }

func wrapVideoAggregatorParallelConvertPad(obj *glib.Object) *VideoAggregatorParallelConvertPad {
	return &VideoAggregatorParallelConvertPad{VideoAggregatorConvertPad: wrapVideoAggregatorConvertPad(obj)}
}

// FromGstVideoAggregatorParallelConvertPadUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoAggregatorParallelConvertPadUnsafeNone(ptr unsafe.Pointer) *VideoAggregatorParallelConvertPad {
	if ptr == nil {
		return nil
	}
	return wrapVideoAggregatorParallelConvertPad(glib.TransferNone(ptr))
}

// FromGstVideoAggregatorParallelConvertPadUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoAggregatorParallelConvertPadUnsafeFull(ptr unsafe.Pointer) *VideoAggregatorParallelConvertPad {
	if ptr == nil {
		return nil
	}
	return wrapVideoAggregatorParallelConvertPad(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoAggregatorParallelConvertPad pointer.
func (v *VideoAggregatorParallelConvertPad) Instance() *C.GstVideoAggregatorParallelConvertPad { return C.toGstVideoAggregatorParallelConvertPad(v.Unsafe()) }

type VideoBufferPool struct{ BufferPool *gst.BufferPool }

func wrapVideoBufferPool(obj *glib.Object) *VideoBufferPool {
	return &VideoBufferPool{BufferPool: gst.FromGstBufferPoolUnsafeNone(unsafe.Pointer(obj.GObject))}
}

// FromGstVideoBufferPoolUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoBufferPoolUnsafeNone(ptr unsafe.Pointer) *VideoBufferPool {
	if ptr == nil {
		return nil
	}
	return wrapVideoBufferPool(glib.TransferNone(ptr))
}

// FromGstVideoBufferPoolUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoBufferPoolUnsafeFull(ptr unsafe.Pointer) *VideoBufferPool {
	if ptr == nil {
		return nil
	}
	return wrapVideoBufferPool(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoBufferPool pointer.
func (v *VideoBufferPool) Instance() *C.GstVideoBufferPool { return C.toGstVideoBufferPool(v.Unsafe()) }

// VideoDecoder:
// This base class is for video decoders turning encoded data into raw video
// frames.
// 
// The GstVideoDecoder base class and derived subclasses should cooperate as
// follows:
// 
// ## Configuration
// 
//   * Initially, GstVideoDecoder calls @start when the decoder element
//     is activated, which allows the subclass to perform any global setup.
// 
//   * GstVideoDecoder calls @set_format to inform the subclass of caps
//     describing input video data that it is about to receive, including
//     possibly configuration data.
//     While unlikely, it might be called more than once, if changing input
//     parameters require reconfiguration.
// 
//   * Incoming data buffers are processed as needed, described in Data
//     Processing below.
// 
//   * GstVideoDecoder calls @stop at end of all processing.
// 
// ## Data processing
// 
//   * The base class gathers input data, and optionally allows subclass
//     to parse this into subsequently manageable chunks, typically
//     corresponding to and referred to as 'frames'.
// 
//   * Each input frame is provided in turn to the subclass' @handle_frame
//     callback.
//   * When the subclass enables the subframe mode with `gst_video_decoder_set_subframe_mode`,
//     the base class will provide to the subclass the same input frame with
//     different input buffers to the subclass @handle_frame
//     callback. During this call, the subclass needs to take
//     ownership of the input_buffer as @GstVideoCodecFrame.input_buffer
//     will have been changed before the next subframe buffer is received.
//     The subclass will call `gst_video_decoder_have_last_subframe`
//     when a new input frame can be created by the base class.
//     Every subframe will share the same @GstVideoCodecFrame.output_buffer
//     to write the decoding result. The subclass is responsible to protect
//     its access.
// 
//   * If codec processing results in decoded data, the subclass should call
//     @gst_video_decoder_finish_frame to have decoded data pushed
//     downstream. In subframe mode
//     the subclass should call @gst_video_decoder_finish_subframe until the
//     last subframe where it should call @gst_video_decoder_finish_frame.
//     The subclass can detect the last subframe using GST_VIDEO_BUFFER_FLAG_MARKER
//     on buffers or using its own logic to collect the subframes.
//     In case of decoding failure, the subclass must call
//     @gst_video_decoder_drop_frame or @gst_video_decoder_drop_subframe,
//     to allow the base class to do timestamp and offset tracking, and possibly
//     to requeue the frame for a later attempt in the case of reverse playback.
// 
// ## Shutdown phase
// 
//   * The GstVideoDecoder class calls @stop to inform the subclass that data
//     parsing will be stopped.
// 
// ## Additional Notes
// 
//   * Seeking/Flushing
// 
//     * When the pipeline is seeked or otherwise flushed, the subclass is
//       informed via a call to its @reset callback, with the hard parameter
//       set to true. This indicates the subclass should drop any internal data
//       queues and timestamps and prepare for a fresh set of buffers to arrive
//       for parsing and decoding.
// 
//   * End Of Stream
// 
//     * At end-of-stream, the subclass @parse function may be called some final
//       times with the at_eos parameter set to true, indicating that the element
//       should not expect any more data to be arriving, and it should parse and
//       remaining frames and call gst_video_decoder_have_frame() if possible.
// 
// The subclass is responsible for providing pad template caps for
// source and sink pads. The pads need to be named "sink" and "src". It also
// needs to provide information about the output caps, when they are known.
// This may be when the base class calls the subclass' @set_format function,
// though it might be during decoding, before calling
// @gst_video_decoder_finish_frame. This is done via
// @gst_video_decoder_set_output_state
// 
// The subclass is also responsible for providing (presentation) timestamps
// (likely based on corresponding input ones).  If that is not applicable
// or possible, the base class provides limited framerate based interpolation.
// 
// Similarly, the base class provides some limited (legacy) seeking support
// if specifically requested by the subclass, as full-fledged support
// should rather be left to upstream demuxer, parser or alike.  This simple
// approach caters for seeking and duration reporting using estimated input
// bitrates. To enable it, a subclass should call
// @gst_video_decoder_set_estimate_rate to enable handling of incoming
// byte-streams.
// 
// The base class provides some support for reverse playback, in particular
// in case incoming data is not packetized or upstream does not provide
// fragments on keyframe boundaries.  However, the subclass should then be
// prepared for the parsing and frame processing stage to occur separately
// (in normal forward processing, the latter immediately follows the former),
// The subclass also needs to ensure the parsing stage properly marks
// keyframes, unless it knows the upstream elements will do so properly for
// incoming data.
// 
// The bare minimum that a functional subclass needs to implement is:
// 
//   * Provide pad templates
//   * Inform the base class of output caps via
//      @gst_video_decoder_set_output_state
// 
//   * Parse input data, if it is not considered packetized from upstream
//      Data will be provided to @parse which should invoke
//      @gst_video_decoder_add_to_frame and @gst_video_decoder_have_frame to
//      separate the data belonging to each video frame.
// 
//   * Accept data in @handle_frame and provide decoded results to
//      @gst_video_decoder_finish_frame, or call @gst_video_decoder_drop_frame.
type VideoDecoder struct{ Element *gst.Element }

func wrapVideoDecoder(obj *glib.Object) *VideoDecoder {
	return &VideoDecoder{Element: gst.FromGstElementUnsafeNone(unsafe.Pointer(obj.GObject))}
}

// FromGstVideoDecoderUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoDecoderUnsafeNone(ptr unsafe.Pointer) *VideoDecoder {
	if ptr == nil {
		return nil
	}
	return wrapVideoDecoder(glib.TransferNone(ptr))
}

// FromGstVideoDecoderUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoDecoderUnsafeFull(ptr unsafe.Pointer) *VideoDecoder {
	if ptr == nil {
		return nil
	}
	return wrapVideoDecoder(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoDecoder pointer.
func (v *VideoDecoder) Instance() *C.GstVideoDecoder { return C.toGstVideoDecoder(v.Unsafe()) }

// VideoDmabufPool: Using #GstUdmabufAllocator, setting defaults and implementing implicit sync.
type VideoDmabufPool struct{ VideoBufferPool *VideoBufferPool }

func wrapVideoDmabufPool(obj *glib.Object) *VideoDmabufPool {
	return &VideoDmabufPool{VideoBufferPool: wrapVideoBufferPool(obj)}
}

// FromGstVideoDmabufPoolUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoDmabufPoolUnsafeNone(ptr unsafe.Pointer) *VideoDmabufPool {
	if ptr == nil {
		return nil
	}
	return wrapVideoDmabufPool(glib.TransferNone(ptr))
}

// FromGstVideoDmabufPoolUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoDmabufPoolUnsafeFull(ptr unsafe.Pointer) *VideoDmabufPool {
	if ptr == nil {
		return nil
	}
	return wrapVideoDmabufPool(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoDmabufPool pointer.
func (v *VideoDmabufPool) Instance() *C.GstVideoDmabufPool { return C.toGstVideoDmabufPool(v.Unsafe()) }

// VideoEncoder:
// This base class is for video encoders turning raw video into
// encoded video data.
// 
// GstVideoEncoder and subclass should cooperate as follows.
// 
// ## Configuration
// 
//   * Initially, GstVideoEncoder calls @start when the encoder element
//     is activated, which allows subclass to perform any global setup.
//   * GstVideoEncoder calls @set_format to inform subclass of the format
//     of input video data that it is about to receive.  Subclass should
//     setup for encoding and configure base class as appropriate
//     (e.g. latency). While unlikely, it might be called more than once,
//     if changing input parameters require reconfiguration.  Baseclass
//     will ensure that processing of current configuration is finished.
//   * GstVideoEncoder calls @stop at end of all processing.
// 
// ## Data processing
// 
//     * Base class collects input data and metadata into a frame and hands
//       this to subclass' @handle_frame.
// 
//     * If codec processing results in encoded data, subclass should call
//       @gst_video_encoder_finish_frame to have encoded data pushed
//       downstream.
// 
//     * If implemented, baseclass calls subclass @pre_push just prior to
//       pushing to allow subclasses to modify some metadata on the buffer.
//       If it returns GST_FLOW_OK, the buffer is pushed downstream.
// 
//     * GstVideoEncoderClass will handle both srcpad and sinkpad events.
//       Sink events will be passed to subclass if @event callback has been
//       provided.
// 
// ## Shutdown phase
// 
//   * GstVideoEncoder class calls @stop to inform the subclass that data
//     parsing will be stopped.
// 
// Subclass is responsible for providing pad template caps for
// source and sink pads. The pads need to be named "sink" and "src". It should
// also be able to provide fixed src pad caps in @getcaps by the time it calls
// @gst_video_encoder_finish_frame.
// 
// Things that subclass need to take care of:
// 
//   * Provide pad templates
//   * Provide source pad caps before pushing the first buffer
//   * Accept data in @handle_frame and provide encoded results to
//      @gst_video_encoder_finish_frame.
// 
// 
// The #GstVideoEncoder:qos property will enable the Quality-of-Service
// features of the encoder which gather statistics about the real-time
// performance of the downstream elements. If enabled, subclasses can
// use gst_video_encoder_get_max_encode_time() to check if input frames
// are already late and drop them right away to give a chance to the
// pipeline to catch up.
type VideoEncoder struct{ Element *gst.Element }

func wrapVideoEncoder(obj *glib.Object) *VideoEncoder {
	return &VideoEncoder{Element: gst.FromGstElementUnsafeNone(unsafe.Pointer(obj.GObject))}
}

// FromGstVideoEncoderUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoEncoderUnsafeNone(ptr unsafe.Pointer) *VideoEncoder {
	if ptr == nil {
		return nil
	}
	return wrapVideoEncoder(glib.TransferNone(ptr))
}

// FromGstVideoEncoderUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoEncoderUnsafeFull(ptr unsafe.Pointer) *VideoEncoder {
	if ptr == nil {
		return nil
	}
	return wrapVideoEncoder(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoEncoder pointer.
func (v *VideoEncoder) Instance() *C.GstVideoEncoder { return C.toGstVideoEncoder(v.Unsafe()) }

// VideoFilter:
// Provides useful functions and a base class for video filters.
// 
// The videofilter will by default enable QoS on the parent GstBaseTransform
// to implement frame dropping.
type VideoFilter struct{ BaseTransform *base.BaseTransform }

func wrapVideoFilter(obj *glib.Object) *VideoFilter {
	return &VideoFilter{BaseTransform: base.FromGstBaseTransformUnsafeNone(unsafe.Pointer(obj.GObject))}
}

// FromGstVideoFilterUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoFilterUnsafeNone(ptr unsafe.Pointer) *VideoFilter {
	if ptr == nil {
		return nil
	}
	return wrapVideoFilter(glib.TransferNone(ptr))
}

// FromGstVideoFilterUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoFilterUnsafeFull(ptr unsafe.Pointer) *VideoFilter {
	if ptr == nil {
		return nil
	}
	return wrapVideoFilter(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoFilter pointer.
func (v *VideoFilter) Instance() *C.GstVideoFilter { return C.toGstVideoFilter(v.Unsafe()) }

// VideoMultiviewFlagsSet: See #GstVideoMultiviewFlags.
type VideoMultiviewFlagsSet struct{ FlagSet *gst.FlagSet }

func wrapVideoMultiviewFlagsSet(obj *glib.Object) *VideoMultiviewFlagsSet {
	return &VideoMultiviewFlagsSet{FlagSet: gst.FromGstFlagSetUnsafeNone(unsafe.Pointer(obj.GObject))}
}

// FromGstVideoMultiviewFlagsSetUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoMultiviewFlagsSetUnsafeNone(ptr unsafe.Pointer) *VideoMultiviewFlagsSet {
	if ptr == nil {
		return nil
	}
	return wrapVideoMultiviewFlagsSet(glib.TransferNone(ptr))
}

// FromGstVideoMultiviewFlagsSetUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoMultiviewFlagsSetUnsafeFull(ptr unsafe.Pointer) *VideoMultiviewFlagsSet {
	if ptr == nil {
		return nil
	}
	return wrapVideoMultiviewFlagsSet(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoMultiviewFlagsSet pointer.
func (v *VideoMultiviewFlagsSet) Instance() *C.GstVideoMultiviewFlagsSet { return C.toGstVideoMultiviewFlagsSet(v.Unsafe()) }

// VideoSink:
// Provides useful functions and a base class for video sinks.
// 
// GstVideoSink will configure the default base sink to drop frames that
// arrive later than 20ms as this is considered the default threshold for
// observing out-of-sync frames.
type VideoSink struct{ BaseSink *base.BaseSink }

func wrapVideoSink(obj *glib.Object) *VideoSink {
	return &VideoSink{BaseSink: base.FromGstBaseSinkUnsafeNone(unsafe.Pointer(obj.GObject))}
}

// FromGstVideoSinkUnsafeNone wraps the pointer with transfer-none semantics.
func FromGstVideoSinkUnsafeNone(ptr unsafe.Pointer) *VideoSink {
	if ptr == nil {
		return nil
	}
	return wrapVideoSink(glib.TransferNone(ptr))
}

// FromGstVideoSinkUnsafeFull wraps the pointer with transfer-full semantics.
func FromGstVideoSinkUnsafeFull(ptr unsafe.Pointer) *VideoSink {
	if ptr == nil {
		return nil
	}
	return wrapVideoSink(glib.TransferFull(ptr))
}

// Instance returns the native C GstVideoSink pointer.
func (v *VideoSink) Instance() *C.GstVideoSink { return C.toGstVideoSink(v.Unsafe()) }

