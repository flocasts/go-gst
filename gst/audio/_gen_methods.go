// Code generated by gst-bindgen. DO NOT EDIT.

package audio

/*
#include "gst.go.h"
*/
import "C"

import (
	"unsafe"
	
	gst "github.com/go-gst/go-gst/gst"
)

func (a *AudioAggregator) SetSinkCaps(pad *AudioAggregatorPad, caps *gst.Caps) {
	C.gst_audio_aggregator_set_sink_caps(a.Instance(), pad.Instance(), caps.Instance())
}

// CreateRingbuffer: Create and return the #GstAudioRingBuffer for @sink. This function will call the ::create_ringbuf...
func (a *AudioBaseSink) CreateRingbuffer() *AudioRingBuffer {
	cResult := C.gst_audio_base_sink_create_ringbuffer(a.Instance())
	if cResult == nil {
		return nil
	}
	return FromGstAudioRingBufferUnsafeNone(unsafe.Pointer(cResult))
}

// GetAlignmentThreshold: Get the current alignment threshold, in nanoseconds, used by @sink.
func (a *AudioBaseSink) GetAlignmentThreshold() {
	C.gst_audio_base_sink_get_alignment_threshold(a.Instance())
}

// GetDiscontWait: Get the current discont wait, in nanoseconds, used by @sink.
func (a *AudioBaseSink) GetDiscontWait() {
	C.gst_audio_base_sink_get_discont_wait(a.Instance())
}

// GetDriftTolerance: Get the current drift tolerance, in microseconds, used by @sink.
func (a *AudioBaseSink) GetDriftTolerance() int64 {
	cResult := C.gst_audio_base_sink_get_drift_tolerance(a.Instance())
	return int64(cResult)
}

// GetProvideClock: Queries whether @sink will provide a clock or not. See also gst_audio_base_sink_set_provide_clock.
func (a *AudioBaseSink) GetProvideClock() bool {
	cResult := C.gst_audio_base_sink_get_provide_clock(a.Instance())
	return int(cResult) > 0
}

// GetSlaveMethod: Get the current slave method used by @sink.
func (a *AudioBaseSink) GetSlaveMethod() AudioBaseSinkSlaveMethod {
	cResult := C.gst_audio_base_sink_get_slave_method(a.Instance())
	return AudioBaseSinkSlaveMethod(cResult)
}

// ReportDeviceFailure: Informs this base class that the audio output device has failed for some reason, causing a discon...
func (a *AudioBaseSink) ReportDeviceFailure() {
	C.gst_audio_base_sink_report_device_failure(a.Instance())
}

// SetAlignmentThreshold: Controls the sink's alignment threshold.
func (a *AudioBaseSink) SetAlignmentThreshold(alignment_threshold unsafe.Pointer) {
	C.gst_audio_base_sink_set_alignment_threshold(a.Instance(), alignment_threshold)
}

// SetDiscontWait: Controls how long the sink will wait before creating a discontinuity.
func (a *AudioBaseSink) SetDiscontWait(discont_wait unsafe.Pointer) {
	C.gst_audio_base_sink_set_discont_wait(a.Instance(), discont_wait)
}

// SetDriftTolerance: Controls the sink's drift tolerance.
func (a *AudioBaseSink) SetDriftTolerance(drift_tolerance int64) {
	C.gst_audio_base_sink_set_drift_tolerance(a.Instance(), C.gint64(drift_tolerance))
}

// SetProvideClock: Controls whether @sink will provide a clock or not. If @provide is %TRUE, gst_element_provide_clo...
func (a *AudioBaseSink) SetProvideClock(provide bool) {
	C.gst_audio_base_sink_set_provide_clock(a.Instance(), gboolean(provide))
}

// SetSlaveMethod: Controls how clock slaving will be performed in @sink.
func (a *AudioBaseSink) SetSlaveMethod(method AudioBaseSinkSlaveMethod) {
	C.gst_audio_base_sink_set_slave_method(a.Instance(), C.GstAudioBaseSinkSlaveMethod(method))
}

// CreateRingbuffer: Create and return the #GstAudioRingBuffer for @src. This function will call the ::create_ringbuff...
func (a *AudioBaseSrc) CreateRingbuffer() *AudioRingBuffer {
	cResult := C.gst_audio_base_src_create_ringbuffer(a.Instance())
	if cResult == nil {
		return nil
	}
	return FromGstAudioRingBufferUnsafeNone(unsafe.Pointer(cResult))
}

// GetProvideClock: Queries whether @src will provide a clock or not. See also gst_audio_base_src_set_provide_clock.
func (a *AudioBaseSrc) GetProvideClock() bool {
	cResult := C.gst_audio_base_src_get_provide_clock(a.Instance())
	return int(cResult) > 0
}

// GetSlaveMethod: Get the current slave method used by @src.
func (a *AudioBaseSrc) GetSlaveMethod() AudioBaseSrcSlaveMethod {
	cResult := C.gst_audio_base_src_get_slave_method(a.Instance())
	return AudioBaseSrcSlaveMethod(cResult)
}

// SetProvideClock: Controls whether @src will provide a clock or not. If @provide is %TRUE, gst_element_provide_cloc...
func (a *AudioBaseSrc) SetProvideClock(provide bool) {
	C.gst_audio_base_src_set_provide_clock(a.Instance(), gboolean(provide))
}

// SetSlaveMethod: Controls how clock slaving will be performed in @src.
func (a *AudioBaseSrc) SetSlaveMethod(method AudioBaseSrcSlaveMethod) {
	C.gst_audio_base_src_set_slave_method(a.Instance(), C.GstAudioBaseSrcSlaveMethod(method))
}

// AddTrack: CDDA sources use this function from their start vfunc to announce the available data and audio tr...
func (a *AudioCdSrc) AddTrack(track AudioCdSrcTrack) bool {
	cResult := C.gst_audio_cd_src_add_track(a.Instance(), track)
	return int(cResult) > 0
}

// Adjust: Adjust @time with the internal offset of the audio clock.
func (a *AudioClock) Adjust(time unsafe.Pointer) {
	C.gst_audio_clock_adjust(a.Instance(), time)
}

// GetTime: Report the time as returned by the #GstAudioClockGetTimeFunc without applying any offsets.
func (a *AudioClock) GetTime() {
	C.gst_audio_clock_get_time(a.Instance())
}

// Invalidate: Invalidate the clock function. Call this function when the provided #GstAudioClockGetTimeFunc can...
func (a *AudioClock) Invalidate() {
	C.gst_audio_clock_invalidate(a.Instance())
}

// Reset: Inform @clock that future calls to #GstAudioClockGetTimeFunc will return values starting from @ti...
func (a *AudioClock) Reset(time unsafe.Pointer) {
	C.gst_audio_clock_reset(a.Instance(), time)
}

// AllocateOutputBuffer: Helper function that allocates a buffer to hold an audio frame for @dec's current output format.
func (a *AudioDecoder) AllocateOutputBuffer(size uint) *gst.Buffer {
	cResult := C.gst_audio_decoder_allocate_output_buffer(a.Instance(), C.gsize(size))
	return gst.FromGstBufferUnsafeFull(unsafe.Pointer(cResult))
}

// FinishFrame: Collects decoded data and pushes it downstream.  @buf may be NULL in which case the indicated num...
func (a *AudioDecoder) FinishFrame(buf *gst.Buffer, frames int) gst.FlowReturn {
	cResult := C.gst_audio_decoder_finish_frame(a.Instance(), buf.Instance(), C.gint(frames))
	return gst.FlowReturn(cResult)
}

// FinishSubframe: Collects decoded data and pushes it downstream. This function may be called multiple times for a ...
func (a *AudioDecoder) FinishSubframe(buf *gst.Buffer) gst.FlowReturn {
	cResult := C.gst_audio_decoder_finish_subframe(a.Instance(), buf.Instance())
	return gst.FlowReturn(cResult)
}

// GetAllocator: Lets #GstAudioDecoder sub-classes to know the memory @allocator used by the base class and its @p...
func (a *AudioDecoder) GetAllocator() {
	C.gst_audio_decoder_get_allocator(a.Instance())
}

func (a *AudioDecoder) GetAudioInfo() AudioInfo {
	cResult := C.gst_audio_decoder_get_audio_info(a.Instance())
	return cResult
}

func (a *AudioDecoder) GetDelay() int {
	cResult := C.gst_audio_decoder_get_delay(a.Instance())
	return int(cResult)
}

// GetDrainable: Queries decoder drain handling.
func (a *AudioDecoder) GetDrainable() bool {
	cResult := C.gst_audio_decoder_get_drainable(a.Instance())
	return int(cResult) > 0
}

func (a *AudioDecoder) GetEstimateRate() int {
	cResult := C.gst_audio_decoder_get_estimate_rate(a.Instance())
	return int(cResult)
}

// GetLatency: Sets the variables pointed to by @min and @max to the currently configured latency.
func (a *AudioDecoder) GetLatency() {
	C.gst_audio_decoder_get_latency(a.Instance())
}

func (a *AudioDecoder) GetMaxErrors() int {
	cResult := C.gst_audio_decoder_get_max_errors(a.Instance())
	return int(cResult)
}

// GetMinLatency: Queries decoder's latency aggregation.
func (a *AudioDecoder) GetMinLatency() {
	C.gst_audio_decoder_get_min_latency(a.Instance())
}

// GetNeedsFormat: Queries decoder required format handling.
func (a *AudioDecoder) GetNeedsFormat() bool {
	cResult := C.gst_audio_decoder_get_needs_format(a.Instance())
	return int(cResult) > 0
}

// GetParseState: Return current parsing (sync and eos) state.
func (a *AudioDecoder) GetParseState() {
	C.gst_audio_decoder_get_parse_state(a.Instance())
}

// GetPlc: Queries decoder packet loss concealment handling.
func (a *AudioDecoder) GetPlc() bool {
	cResult := C.gst_audio_decoder_get_plc(a.Instance())
	return int(cResult) > 0
}

func (a *AudioDecoder) GetPlcAware() int {
	cResult := C.gst_audio_decoder_get_plc_aware(a.Instance())
	return int(cResult)
}

// GetTolerance: Queries current audio jitter tolerance threshold.
func (a *AudioDecoder) GetTolerance() {
	C.gst_audio_decoder_get_tolerance(a.Instance())
}

// MergeTags: Sets the audio decoder tags and how they should be merged with any upstream stream tags. This wil...
func (a *AudioDecoder) MergeTags(tags *gst.TagList, mode gst.TagMergeMode) {
	C.gst_audio_decoder_merge_tags(a.Instance(), tags.Instance(), C.GstTagMergeMode(mode))
}

// Negotiate: Negotiate with downstream elements to currently configured #GstAudioInfo. Unmark GST_PAD_FLAG_NEE...
func (a *AudioDecoder) Negotiate() bool {
	cResult := C.gst_audio_decoder_negotiate(a.Instance())
	return int(cResult) > 0
}

// ProxyGetcaps: Returns caps that express @caps (or sink template caps if @caps == NULL) restricted to rate/chann...
func (a *AudioDecoder) ProxyGetcaps(caps *gst.Caps, filter *gst.Caps) *gst.Caps {
	cResult := C.gst_audio_decoder_proxy_getcaps(a.Instance(), caps.Instance(), filter.Instance())
	return gst.FromGstCapsUnsafeFull(unsafe.Pointer(cResult))
}

// SetAllocationCaps: Sets a caps in allocation query which are different from the set pad's caps. Use this function be...
func (a *AudioDecoder) SetAllocationCaps(allocation_caps *gst.Caps) {
	C.gst_audio_decoder_set_allocation_caps(a.Instance(), allocation_caps.Instance())
}

// SetDrainable: Configures decoder drain handling.  If drainable, subclass might be handed a NULL buffer to have ...
func (a *AudioDecoder) SetDrainable(enabled bool) {
	C.gst_audio_decoder_set_drainable(a.Instance(), gboolean(enabled))
}

// SetEstimateRate: Allows baseclass to perform byte to time estimated conversion.
func (a *AudioDecoder) SetEstimateRate(enabled bool) {
	C.gst_audio_decoder_set_estimate_rate(a.Instance(), gboolean(enabled))
}

// SetLatency: Sets decoder latency. If the provided values changed from previously provided ones, this will als...
func (a *AudioDecoder) SetLatency(min unsafe.Pointer, max unsafe.Pointer) {
	C.gst_audio_decoder_set_latency(a.Instance(), min, max)
}

// SetMaxErrors: Sets numbers of tolerated decoder errors, where a tolerated one is then only warned about, but mo...
func (a *AudioDecoder) SetMaxErrors(num int) {
	C.gst_audio_decoder_set_max_errors(a.Instance(), C.gint(num))
}

// SetMinLatency: Sets decoder minimum aggregation latency.  MT safe.
func (a *AudioDecoder) SetMinLatency(num unsafe.Pointer) {
	C.gst_audio_decoder_set_min_latency(a.Instance(), num)
}

// SetNeedsFormat: Configures decoder format needs.  If enabled, subclass needs to be negotiated with format caps be...
func (a *AudioDecoder) SetNeedsFormat(enabled bool) {
	C.gst_audio_decoder_set_needs_format(a.Instance(), gboolean(enabled))
}

// SetOutputCaps: Configure output caps on the srcpad of @dec. Similar to gst_audio_decoder_set_output_format(), bu...
func (a *AudioDecoder) SetOutputCaps(caps *gst.Caps) bool {
	cResult := C.gst_audio_decoder_set_output_caps(a.Instance(), caps.Instance())
	return int(cResult) > 0
}

// SetOutputFormat: Configure output info on the srcpad of @dec.
func (a *AudioDecoder) SetOutputFormat(info AudioInfo) bool {
	cResult := C.gst_audio_decoder_set_output_format(a.Instance(), info)
	return int(cResult) > 0
}

// SetPlc: Enable or disable decoder packet loss concealment, provided subclass and codec are capable and al...
func (a *AudioDecoder) SetPlc(enabled bool) {
	C.gst_audio_decoder_set_plc(a.Instance(), gboolean(enabled))
}

// SetPlcAware: Indicates whether or not subclass handles packet loss concealment (plc).
func (a *AudioDecoder) SetPlcAware(plc bool) {
	C.gst_audio_decoder_set_plc_aware(a.Instance(), gboolean(plc))
}

// SetTolerance: Configures decoder audio jitter tolerance threshold.  MT safe.
func (a *AudioDecoder) SetTolerance(tolerance unsafe.Pointer) {
	C.gst_audio_decoder_set_tolerance(a.Instance(), tolerance)
}

// SetUseDefaultPadAcceptcaps: Lets #GstAudioDecoder sub-classes decide if they want the sink pad to use the default pad query h...
func (a *AudioDecoder) SetUseDefaultPadAcceptcaps(use bool) {
	C.gst_audio_decoder_set_use_default_pad_acceptcaps(a.Instance(), gboolean(use))
}

// AllocateOutputBuffer: Helper function that allocates a buffer to hold an encoded audio frame for @enc's current output ...
func (a *AudioEncoder) AllocateOutputBuffer(size uint) *gst.Buffer {
	cResult := C.gst_audio_encoder_allocate_output_buffer(a.Instance(), C.gsize(size))
	return gst.FromGstBufferUnsafeFull(unsafe.Pointer(cResult))
}

// FinishFrame: Collects encoded data and pushes encoded data downstream. Source pad caps must be set when this i...
func (a *AudioEncoder) FinishFrame(buffer *gst.Buffer, samples int) gst.FlowReturn {
	cResult := C.gst_audio_encoder_finish_frame(a.Instance(), buffer.Instance(), C.gint(samples))
	return gst.FlowReturn(cResult)
}

// GetAllocator: Lets #GstAudioEncoder sub-classes to know the memory @allocator used by the base class and its @p...
func (a *AudioEncoder) GetAllocator() {
	C.gst_audio_encoder_get_allocator(a.Instance())
}

func (a *AudioEncoder) GetAudioInfo() AudioInfo {
	cResult := C.gst_audio_encoder_get_audio_info(a.Instance())
	return cResult
}

// GetDrainable: Queries encoder drain handling.
func (a *AudioEncoder) GetDrainable() bool {
	cResult := C.gst_audio_encoder_get_drainable(a.Instance())
	return int(cResult) > 0
}

func (a *AudioEncoder) GetFrameMax() int {
	cResult := C.gst_audio_encoder_get_frame_max(a.Instance())
	return int(cResult)
}

func (a *AudioEncoder) GetFrameSamplesMax() int {
	cResult := C.gst_audio_encoder_get_frame_samples_max(a.Instance())
	return int(cResult)
}

func (a *AudioEncoder) GetFrameSamplesMin() int {
	cResult := C.gst_audio_encoder_get_frame_samples_min(a.Instance())
	return int(cResult)
}

// GetHardMin: Queries encoder hard minimum handling.
func (a *AudioEncoder) GetHardMin() bool {
	cResult := C.gst_audio_encoder_get_hard_min(a.Instance())
	return int(cResult) > 0
}

func (a *AudioEncoder) GetHardResync() bool {
	cResult := C.gst_audio_encoder_get_hard_resync(a.Instance())
	return int(cResult) > 0
}

// GetLatency: Sets the variables pointed to by @min and @max to the currently configured latency.
func (a *AudioEncoder) GetLatency() {
	C.gst_audio_encoder_get_latency(a.Instance())
}

func (a *AudioEncoder) GetLookahead() int {
	cResult := C.gst_audio_encoder_get_lookahead(a.Instance())
	return int(cResult)
}

// GetMarkGranule: Queries if the encoder will handle granule marking.
func (a *AudioEncoder) GetMarkGranule() bool {
	cResult := C.gst_audio_encoder_get_mark_granule(a.Instance())
	return int(cResult) > 0
}

// GetPerfectTimestamp: Queries encoder perfect timestamp behaviour.
func (a *AudioEncoder) GetPerfectTimestamp() bool {
	cResult := C.gst_audio_encoder_get_perfect_timestamp(a.Instance())
	return int(cResult) > 0
}

// GetTolerance: Queries current audio jitter tolerance threshold.
func (a *AudioEncoder) GetTolerance() {
	C.gst_audio_encoder_get_tolerance(a.Instance())
}

// MergeTags: Sets the audio encoder tags and how they should be merged with any upstream stream tags. This wil...
func (a *AudioEncoder) MergeTags(tags *gst.TagList, mode gst.TagMergeMode) {
	C.gst_audio_encoder_merge_tags(a.Instance(), tags.Instance(), C.GstTagMergeMode(mode))
}

// Negotiate: Negotiate with downstream elements to currently configured #GstCaps. Unmark GST_PAD_FLAG_NEED_REC...
func (a *AudioEncoder) Negotiate() bool {
	cResult := C.gst_audio_encoder_negotiate(a.Instance())
	return int(cResult) > 0
}

// ProxyGetcaps: Returns caps that express @caps (or sink template caps if @caps == NULL) restricted to channel/ra...
func (a *AudioEncoder) ProxyGetcaps(caps *gst.Caps, filter *gst.Caps) *gst.Caps {
	cResult := C.gst_audio_encoder_proxy_getcaps(a.Instance(), caps.Instance(), filter.Instance())
	return gst.FromGstCapsUnsafeFull(unsafe.Pointer(cResult))
}

// SetAllocationCaps: Sets a caps in allocation query which are different from the set pad's caps. Use this function be...
func (a *AudioEncoder) SetAllocationCaps(allocation_caps *gst.Caps) {
	C.gst_audio_encoder_set_allocation_caps(a.Instance(), allocation_caps.Instance())
}

// SetDrainable: Configures encoder drain handling.  If drainable, subclass might be handed a NULL buffer to have ...
func (a *AudioEncoder) SetDrainable(enabled bool) {
	C.gst_audio_encoder_set_drainable(a.Instance(), gboolean(enabled))
}

// SetFrameMax: Sets max number of frames accepted at once (assumed minimally 1). Requires @frame_samples_min and...
func (a *AudioEncoder) SetFrameMax(num int) {
	C.gst_audio_encoder_set_frame_max(a.Instance(), C.gint(num))
}

// SetFrameSamplesMax: Sets number of samples (per channel) subclass needs to be handed, at most or will be handed all a...
func (a *AudioEncoder) SetFrameSamplesMax(num int) {
	C.gst_audio_encoder_set_frame_samples_max(a.Instance(), C.gint(num))
}

// SetFrameSamplesMin: Sets number of samples (per channel) subclass needs to be handed, at least or will be handed all ...
func (a *AudioEncoder) SetFrameSamplesMin(num int) {
	C.gst_audio_encoder_set_frame_samples_min(a.Instance(), C.gint(num))
}

// SetHardMin: Configures encoder hard minimum handling.  If enabled, subclass will never be handed less samples...
func (a *AudioEncoder) SetHardMin(enabled bool) {
	C.gst_audio_encoder_set_hard_min(a.Instance(), gboolean(enabled))
}

func (a *AudioEncoder) SetHardResync(enabled bool) {
	C.gst_audio_encoder_set_hard_resync(a.Instance(), gboolean(enabled))
}

// SetHeaders: Set the codec headers to be sent downstream whenever requested.
func (a *AudioEncoder) SetHeaders(headers unsafe.Pointer) {
	C.gst_audio_encoder_set_headers(a.Instance(), headers)
}

// SetLatency: Sets encoder latency. If the provided values changed from previously provided ones, this will als...
func (a *AudioEncoder) SetLatency(min unsafe.Pointer, max unsafe.Pointer) {
	C.gst_audio_encoder_set_latency(a.Instance(), min, max)
}

// SetLookahead: Sets encoder lookahead (in units of input rate samples)  Note: This value will be reset to 0 ever...
func (a *AudioEncoder) SetLookahead(num int) {
	C.gst_audio_encoder_set_lookahead(a.Instance(), C.gint(num))
}

// SetMarkGranule: Enable or disable encoder granule handling.  MT safe.
func (a *AudioEncoder) SetMarkGranule(enabled bool) {
	C.gst_audio_encoder_set_mark_granule(a.Instance(), gboolean(enabled))
}

// SetOutputFormat: Configure output caps on the srcpad of @enc.
func (a *AudioEncoder) SetOutputFormat(caps *gst.Caps) bool {
	cResult := C.gst_audio_encoder_set_output_format(a.Instance(), caps.Instance())
	return int(cResult) > 0
}

// SetPerfectTimestamp: Enable or disable encoder perfect output timestamp preference.  MT safe.
func (a *AudioEncoder) SetPerfectTimestamp(enabled bool) {
	C.gst_audio_encoder_set_perfect_timestamp(a.Instance(), gboolean(enabled))
}

// SetTolerance: Configures encoder audio jitter tolerance threshold.  MT safe.
func (a *AudioEncoder) SetTolerance(tolerance unsafe.Pointer) {
	C.gst_audio_encoder_set_tolerance(a.Instance(), tolerance)
}

// Acquire: Allocate the resources for the ringbuffer. This function fills in the data pointer of the ring bu...
func (a *AudioRingBuffer) Acquire(spec AudioRingBufferSpec) bool {
	cResult := C.gst_audio_ring_buffer_acquire(a.Instance(), spec)
	return int(cResult) > 0
}

// Activate: Activate @buf to start or stop pulling data.  MT safe.
func (a *AudioRingBuffer) Activate(active bool) bool {
	cResult := C.gst_audio_ring_buffer_activate(a.Instance(), gboolean(active))
	return int(cResult) > 0
}

// Advance: Subclasses should call this function to notify the fact that @advance segments are now processed ...
func (a *AudioRingBuffer) Advance(advance uint) {
	C.gst_audio_ring_buffer_advance(a.Instance(), C.guint(advance))
}

// Clear: Clear the given segment of the buffer with silence samples. This function is used by subclasses. ...
func (a *AudioRingBuffer) Clear(segment int) {
	C.gst_audio_ring_buffer_clear(a.Instance(), C.gint(segment))
}

// ClearAll: Clear all samples from the ringbuffer.  MT safe.
func (a *AudioRingBuffer) ClearAll() {
	C.gst_audio_ring_buffer_clear_all(a.Instance())
}

// CloseDevice: Close the audio device associated with the ring buffer. The ring buffer should already have been ...
func (a *AudioRingBuffer) CloseDevice() bool {
	cResult := C.gst_audio_ring_buffer_close_device(a.Instance())
	return int(cResult) > 0
}

// Convert: Convert @src_val in @src_fmt to the equivalent value in @dest_fmt. The result will be put in @des...
func (a *AudioRingBuffer) Convert(src_fmt gst.Format, src_val int64, dest_fmt gst.Format) bool {
	cResult := C.gst_audio_ring_buffer_convert(a.Instance(), C.GstFormat(src_fmt), C.gint64(src_val), C.GstFormat(dest_fmt))
	return int(cResult) > 0
}

// Delay: Get the number of samples queued in the audio device. This is usually less than the segment size ...
func (a *AudioRingBuffer) Delay() uint {
	cResult := C.gst_audio_ring_buffer_delay(a.Instance())
	return uint(cResult)
}

// DeviceIsOpen: Checks the status of the device associated with the ring buffer.
func (a *AudioRingBuffer) DeviceIsOpen() bool {
	cResult := C.gst_audio_ring_buffer_device_is_open(a.Instance())
	return int(cResult) > 0
}

// GetSegbase: Gets the current segment base number of the ringbuffer.  MT safe.
func (a *AudioRingBuffer) GetSegbase() uint64 {
	cResult := C.gst_audio_ring_buffer_get_segbase(a.Instance())
	return uint64(cResult)
}

// GetSegdone: Gets the current segment number of the ringbuffer.  MT safe.
func (a *AudioRingBuffer) GetSegdone() uint64 {
	cResult := C.gst_audio_ring_buffer_get_segdone(a.Instance())
	return uint64(cResult)
}

// IsAcquired: Check if the ringbuffer is acquired and ready to use.
func (a *AudioRingBuffer) IsAcquired() bool {
	cResult := C.gst_audio_ring_buffer_is_acquired(a.Instance())
	return int(cResult) > 0
}

// IsActive: Check if @buf is activated.  MT safe.
func (a *AudioRingBuffer) IsActive() bool {
	cResult := C.gst_audio_ring_buffer_is_active(a.Instance())
	return int(cResult) > 0
}

// IsFlushing: Check if @buf is flushing.  MT safe.
func (a *AudioRingBuffer) IsFlushing() bool {
	cResult := C.gst_audio_ring_buffer_is_flushing(a.Instance())
	return int(cResult) > 0
}

// MayStart: Tell the ringbuffer that it is allowed to start playback when the ringbuffer is filled with sampl...
func (a *AudioRingBuffer) MayStart(allowed bool) {
	C.gst_audio_ring_buffer_may_start(a.Instance(), gboolean(allowed))
}

// OpenDevice: Open the audio device associated with the ring buffer. Does not perform any setup on the device. ...
func (a *AudioRingBuffer) OpenDevice() bool {
	cResult := C.gst_audio_ring_buffer_open_device(a.Instance())
	return int(cResult) > 0
}

// Pause: Pause processing samples from the ringbuffer.
func (a *AudioRingBuffer) Pause() bool {
	cResult := C.gst_audio_ring_buffer_pause(a.Instance())
	return int(cResult) > 0
}

// PrepareRead: Returns a pointer to memory where the data from segment @segment can be found. This function is m...
func (a *AudioRingBuffer) PrepareRead() bool {
	cResult := C.gst_audio_ring_buffer_prepare_read(a.Instance())
	return int(cResult) > 0
}

// Read: Read @len samples from the ringbuffer into the memory pointed to by @data. The first sample shoul...
func (a *AudioRingBuffer) Read(sample uint64, data unsafe.Pointer, len uint) uint {
	cResult := C.gst_audio_ring_buffer_read(a.Instance(), C.guint64(sample), data, C.guint(len))
	return uint(cResult)
}

// Release: Free the resources of the ringbuffer.
func (a *AudioRingBuffer) Release() bool {
	cResult := C.gst_audio_ring_buffer_release(a.Instance())
	return int(cResult) > 0
}

// SamplesDone: Get the number of samples that were processed by the ringbuffer since it was last started. This d...
func (a *AudioRingBuffer) SamplesDone() uint64 {
	cResult := C.gst_audio_ring_buffer_samples_done(a.Instance())
	return uint64(cResult)
}

// SetChannelPositions: Tell the ringbuffer about the device's channel positions. This must be called in when the ringbuf...
func (a *AudioRingBuffer) SetChannelPositions(position unsafe.Pointer) {
	C.gst_audio_ring_buffer_set_channel_positions(a.Instance(), position)
}

// SetErrored: Mark the ringbuffer as errored after it has started.  MT safe.
func (a *AudioRingBuffer) SetErrored() {
	C.gst_audio_ring_buffer_set_errored(a.Instance())
}

// SetFlushing: Set the ringbuffer to flushing mode or normal mode.  MT safe.
func (a *AudioRingBuffer) SetFlushing(flushing bool) {
	C.gst_audio_ring_buffer_set_flushing(a.Instance(), gboolean(flushing))
}

// SetSample: Make sure that the next sample written to the device is accounted for as being the @sample sample...
func (a *AudioRingBuffer) SetSample(sample uint64) {
	C.gst_audio_ring_buffer_set_sample(a.Instance(), C.guint64(sample))
}

// SetSegdone: Sets the current segment number of the ringbuffer.  MT safe.
func (a *AudioRingBuffer) SetSegdone(segdone uint64) {
	C.gst_audio_ring_buffer_set_segdone(a.Instance(), C.guint64(segdone))
}

func (a *AudioRingBuffer) SetTimestamp(readseg int, timestamp unsafe.Pointer) {
	C.gst_audio_ring_buffer_set_timestamp(a.Instance(), C.gint(readseg), timestamp)
}

// Start: Start processing samples from the ringbuffer.
func (a *AudioRingBuffer) Start() bool {
	cResult := C.gst_audio_ring_buffer_start(a.Instance())
	return int(cResult) > 0
}

// Stop: Stop processing samples from the ringbuffer.
func (a *AudioRingBuffer) Stop() bool {
	cResult := C.gst_audio_ring_buffer_stop(a.Instance())
	return int(cResult) > 0
}

